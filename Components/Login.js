import React from 'react';
import TemplateLoginRegister from './TemplateLoginRegister';

const Login = ({ navigation }) => {
    return <TemplateLoginRegister navigation={navigation} choice="Register" />;
};

export default Login;
